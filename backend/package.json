{
  "name": "@revsense/backend",
  "version": "0.1.0",
  "private": true,
  "description": "RevSense diagnostic core: schemas, knowledge base, scoring engine, AI provider layer. Pure TypeScript, no server — consumed by the Next.js API route.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.8"
  }
}
