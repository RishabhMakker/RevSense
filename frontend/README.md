# @revsense/frontend

Next.js app for RevSense — UI plus the serverless API layer
(`app/api/diagnose`, `app/api/demo`, `app/api/status`). Domain logic lives in
the sibling [`../backend`](../backend) workspace package.

See the [root README](../README.md) for setup, demo scenario, and deployment.
